import { Helmet } from "react-helmet";

const Home: React.FC = () => {
  return (
    <div>
      <Helmet>
        <title>Admin Home Page</title>
      </Helmet>
      <h2>Home</h2>
    </div>
  );
};

export default Home;
