import { FC } from "react";
import { Helmet } from "react-helmet";

const Login: FC = () => {
  return (
    <div>
      <Helmet>
        <title>Admin Login Page</title>
      </Helmet>
      <h2>Login</h2>
    </div>
  );
};

export default Login;
