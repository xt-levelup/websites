import { FC } from "react";
import { Helmet } from "react-helmet";

const Products: FC = () => {
  return (
    <div>
      <Helmet>
        <title>Products</title>
      </Helmet>
      <h2>Products</h2>
    </div>
  );
};

export default Products;
