import { FC } from "react";
import { Helmet } from "react-helmet";

const Products: FC = () => {
  return (
    <div>
      <Helmet>
        <title>Products</title>
      </Helmet>
      <div>Products</div>
    </div>
  );
};

export default Products;
