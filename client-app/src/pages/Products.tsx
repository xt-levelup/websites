import { Helmet } from "react-helmet";

const Products = () => {
  return (
    <div>
      <Helmet>
        <title>Product List</title>
      </Helmet>
      <div>Products</div>
    </div>
  );
};

export default Products;
